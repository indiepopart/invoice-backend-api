{
  "version": 2,
  "builds": [
    {
      "src": "dist/index.js",
      "use": "@vercel/node",
      "buildCommand": "npx tsc",
      "config": { "includeFiles": ["dist/**"] }
    }
  ],
  "routes": [{ "src": "/(.*)", "dest": "dist/index.js" }]
}
